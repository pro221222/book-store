<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css">
    <title>Document</title>
</head>
<body>
    @RenderBody()
    <div id="test"></div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js" integrity="sha384-QJHtvGhmr9XOIpI6YVutG+2QOK9T+ZnN4kzFN1RtK3zEFEIsxhlmWl5/YESvpZ13" crossorigin="anonymous"></script>

    <script src="https://code.jquery.com/jquery-3.6.3.js"></script>
    @* <script src="https://code.jquery.com/jquery-3.7.0.slim.min.js" integrity="sha256-tG5mcZUtJsZvyKAxYLVXrmjKBVLd6VpVccqz/r4ypFE=" crossorigin="anonymous"></script> *@
    
    <script>
        $(document).ready(function(){
            @* var id = sessionStorage.getItem('Id') *@
            $(".Name").attr("placeholder", sessionStorage.getItem('Name'));
            $(".PhoneNumber").attr("placeholder", sessionStorage.getItem('Phonenumber'));
            @* $(".Password").attr("placeholder", sessionStorage.getItem('Password')); *@
            $(".Address").attr("placeholder", sessionStorage.getItem('Address'));
        })
    $('.btn').click(function(){
        //event.preventDefault();
        var Phone = $('#exampleInputEmail1').val()
        var Pass = $('#exampleInputPassword1').val()
        $.get("http://localhost:5246/User/XuLyLogin",
        { 
            Phone : Phone,
            Pass : Pass
        }, function(data){

          var array = data
          console.log(array[1])
            sessionStorage.setItem('Id',array[0])
            sessionStorage.setItem('Name',array[1])
            sessionStorage.setItem('Phonenumber',array[2])
            sessionStorage.setItem('Address',array[3])
            sessionStorage.setItem('Role',array[4])
        })

    })
    
    </script>
</body>
</html>